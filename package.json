{
  "private": true,
  "name": "hello-html",
  "scripts": {
    "start": "browser-sync start --server public --files public",
    "deploy": "netlify deploy --prod --dir=public",
    "setup-deploy": "app-app --deploy && yarn deploy",
    "postdeploy": "echo 'Your site was deployed to https://hello-html-trentcosta.netlify.com'"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true,
    "jsxSingleQuote": true,
    "useTabs": false
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "prettier": "^2.0.5",
    "stylelint": "^13.6.0",
    "stylelint-config-standard": "^20.0.0"
  },
  "homepage": "https://hello-html-trentcosta.netlify.com"
}